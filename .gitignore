# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
ENV/
*.egg-info/

# ── Databases (NEVER commit SQLite files) ─────────────────────────────────────
instance/
*.sqlite3
*.db

# ── Environment / Secrets (CRITICAL — never commit these) ─────────────────────
.env
.env.production
.env.staging
.env.local
.env.*.local
*.pem
*.key

# ── AWS Credentials ────────────────────────────────────────────────────────────
.aws/

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log
logs/
server.log
notifications.log
training_log.txt
error_log.txt
waitress_output.log
global_500_error.txt

# ── OS details ────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Docker local state ────────────────────────────────────────────────────────
docker_build_log.txt

# ── IDE ───────────────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.bak

# ── Trained ML models (large binary — use S3 or DVC instead) ─────────────────
trained_models/
*.bin
*.pt
# ── Uploads (stored in S3 in production) ─────────────────────────────────────
app/uploads/
STAFF_CREDENTIALS.txt
